# ╔════════════════════════════════════════════════════════════════════════════╗
# ║                           PROVELT - Environment Variables                    ║
# ║                                                                              ║
# ║  Copy this file to .env.local and fill in your values                       ║
# ║  NEVER commit .env.local to version control!                                ║
# ╚════════════════════════════════════════════════════════════════════════════╝

# =============================================================================
# 🗄️  SUPABASE CONFIGURATION
# =============================================================================
# Your Supabase project URL (found in Project Settings > API)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co

# Supabase anon/public key (safe for client-side)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# Supabase service role key (NEVER expose to client!)
# Used for server-side admin operations
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# =============================================================================
# 🔗  SOLANA BLOCKCHAIN CONFIGURATION
# =============================================================================
# Network: 'devnet' for testing, 'mainnet-beta' for production
NEXT_PUBLIC_SOLANA_NETWORK=devnet

# RPC URL - Custom RPC recommended for production
# Free options: https://api.devnet.solana.com
# Recommended: Helius, QuickNode, Alchemy, Triton
NEXT_PUBLIC_SOLANA_RPC_URL=https://api.devnet.solana.com

# Merkle Tree Address (for compressed NFT minting)
# Create one using: https://developers.metaplex.com/bubblegum
# Leave as placeholder until you create one
NEXT_PUBLIC_MERKLE_TREE_ADDRESS=11111111111111111111111111111111

# Collection NFT Address (groups all badge NFTs)
# Create one using Metaplex or leave placeholder
NEXT_PUBLIC_COLLECTION_ADDRESS=11111111111111111111111111111111

# =============================================================================
# 🔐  SERVER-SIDE SECRETS (NEVER EXPOSE TO CLIENT)
# =============================================================================
# Treasury wallet private key for minting badges
# Generate with: solana-keygen new --outfile treasury.json
# Convert to Base58: cat treasury.json | jq -r '.[]' | tr '\n' ',' | base58
# ⚠️  KEEP THIS SECRET! Anyone with this key can spend your SOL
TREASURY_PRIVATE_KEY=your_treasury_wallet_private_key_base58

# =============================================================================
# 🌐  APPLICATION CONFIGURATION
# =============================================================================
# Base URL for your application
NEXT_PUBLIC_APP_URL=http://localhost:3000

# App name displayed in UI and metadata
NEXT_PUBLIC_APP_NAME=PROVELT

# =============================================================================
# 📊  OPTIONAL: ANALYTICS & MONITORING
# =============================================================================
# PostHog Analytics (https://posthog.com)
# NEXT_PUBLIC_POSTHOG_KEY=your_posthog_key
# NEXT_PUBLIC_POSTHOG_HOST=https://app.posthog.com

# Sentry Error Tracking (https://sentry.io)
# SENTRY_DSN=your_sentry_dsn
# SENTRY_AUTH_TOKEN=your_sentry_auth_token

# =============================================================================
# 🔧  DEVELOPMENT HELPERS
# =============================================================================
# Enable verbose logging in development
# DEBUG=true

# Skip wallet verification for testing (development only!)
# NEXT_PUBLIC_SKIP_WALLET_VERIFICATION=false
